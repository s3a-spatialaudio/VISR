# Copyright Institute of Sound and Vibration Research - All rights reserved

ADD_DEFINITIONS( -DCMAKE_CURRENT_SOURCE_DIR="${CMAKE_CURRENT_SOURCE_DIR}" )

ADD_EXECUTABLE( baseline_renderer main.cpp options.hpp options.cpp )

TARGET_LINK_LIBRARIES(baseline_renderer signalflows)
TARGET_LINK_LIBRARIES(baseline_renderer rcl)
TARGET_LINK_LIBRARIES(baseline_renderer pml)
TARGET_LINK_LIBRARIES(baseline_renderer objectmodel)
TARGET_LINK_LIBRARIES(baseline_renderer panning)
TARGET_LINK_LIBRARIES(baseline_renderer rbbl)
TARGET_LINK_LIBRARIES(baseline_renderer ril)
TARGET_LINK_LIBRARIES(baseline_renderer rrl)
TARGET_LINK_LIBRARIES(baseline_renderer efl)
TARGET_LINK_LIBRARIES(baseline_renderer ${PORTAUDIO_LIBRARIES} )
TARGET_LINK_LIBRARIES(baseline_renderer ${SNDFILE_LIBRARY} )
TARGET_LINK_LIBRARIES(baseline_renderer ${Boost_FILESYSTEM_LIBRARY} )
TARGET_LINK_LIBRARIES(baseline_renderer ${Boost_SYSTEM_LIBRARY} )
TARGET_LINK_LIBRARIES(baseline_renderer ${Boost_THREAD_LIBRARY} )
TARGET_LINK_LIBRARIES(baseline_renderer ${Boost_PROGRAM_OPTIONS_LIBRARY} )
TARGET_LINK_LIBRARIES(baseline_renderer ${CMAKE_THREAD_LIBS_INIT} )

IF( BUILD_USE_JACK )
  TARGET_LINK_LIBRARIES(baseline_renderer ${JACK_LIBRARY} )
ENDIF( BUILD_USE_JACK )

TARGET_LINK_LIBRARIES(baseline_renderer ${FFTS_LIBRARY} )
TARGET_LINK_LIBRARIES(baseline_renderer ${KISSFFT_LIBRARY} )
IF( BUILD_USE_FFTW )
  TARGET_LINK_LIBRARIES(baseline_renderer ${FFTW3_LIBRARY} )
  TARGET_LINK_LIBRARIES(baseline_renderer ${FFTW3F_LIBRARY} )
ENDIF( BUILD_USE_FFTW )
IF( BUILD_USE_IPP )
  TARGET_LINK_LIBRARIES(baseline_renderer  ${IPP_LIB_S} ${IPP_LIB_VM} ${IPP_LIB_CORE} )
ENDIF( BUILD_USE_IPP )
