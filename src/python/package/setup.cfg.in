[metadata]
name = visr
# version = 0.13.0
# author = Andreas Franck
author_email = andras.franck@audioscenic.com
description = VISR audio rendering framework

[options]
zip_safe = False
python_requires = >=3.0
install_requires =
    numpy

distclass = Distribution()

include_package_data = True
package_dir =
  = visr
packages = find_namespace:

[options.packages.find]
where = visr

#[options.packages.find]
#include_package_data = True
